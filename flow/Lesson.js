//eslint-disable-next-line
declare type Lesson = {
    id: number,
    //eslint-disable-next-line
    words: Array<Word>,
    title: string,
}

//eslint-disable-next-line
declare type TestLesson = {
    id: number,
    //eslint-disable-next-line
    words: Array<TestWord>,
    title: string
}
