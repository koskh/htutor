.panel,
.separator {
  height: 2rem;
  margin: 0.25rem;
}

.sound-btn {
  width: 24px;
  height: 24px;
  position: absolute;
  top: 50%;
  right: 0px;
  transform: translateY(-50%);
  padding: 0;
  border: 0px;
}
